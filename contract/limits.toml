# The contract's numeric facts.

# Semver: changes to what input is accepted, or to what output a given input
# produces, is a major bump; settling an open question without changing any
# conforming implementation's answer is a minor one.
version = "1.0.0"

[limits]
max_coordinate = 50

max_instructions = 99
